Ссылочные переменные (хранятся в куче(хиппе)):
Сылочные переменных хранят только ссылку на сам объект в памяти
Инициализируются по умолчанию null-ом

Строковые:
String - хранят символы и строки "лалала"

Массив:
Все массивы типов данных
