Класс Object и его методы
finalize() - метод, который срабатывает перед тем как сборщик мусора уничтожит класс
getClass() - возвращает объект
toString() - метод, выводящий информацию об объекте. Может вызываться командой sout
equals - сравнение строк
hasgCode - хешкод объекта