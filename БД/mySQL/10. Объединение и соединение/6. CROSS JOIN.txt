Возвращает все записи обеих таблиц присоединяя их друг с другом
Присоединение каждой строки левой таблицы к каждой строке правой таблицы (декартовое произведение)

Пример:
SELECT * FROM table_a
CROSS JOIN table_b