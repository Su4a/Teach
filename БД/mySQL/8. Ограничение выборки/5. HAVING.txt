Дополнительное условие, которое применяется только к агрегированным результатам для ограничения групп возвращаемых строк
HAVING - условие после группировки

Пример:
SELECT имя_поля, COUNT(*) AS имя_поля2
FROM имя_таблицы
GROUP BY имя_поля
HAVING COUNT(*) > 1
